---
import LazyImage from "./LazyImage.astro";

interface FirstPersonCardProps {
  title?: string;
  imageUrl?: string | null;
  transitionName?: string;
  href?: string;
}

const {
  title = "Без названия",
  imageUrl,
  transitionName,
  href = "#",
} = Astro.props as FirstPersonCardProps;

const imageLoading: "lazy" | "eager" = imageUrl ? "lazy" : "eager";
---

<div class="linked-image-hover w-full">
  <div
    class="relative flex aspect-2/3 w-full flex-row overflow-hidden bg-base-100 border-6 border-gray-400"
  >
    <div class="flex w-1/5 items-start justify-center">
      <span
        class="[writing-mode:vertical-rl] rotate-180 whitespace-nowrap text-xs text-accent font-semibold uppercase tracking-[0.25em] px-2 py-1 font-['Inter']"
      >
        от первого лица
      </span>
    </div>
    <div class="w-4/5 grid grid-rows-[5fr_7fr]">
      <div class="pt-2 pr-2">
        <h2
          class="font-old_standard leading-[1.15] text-base-content text-[22px] font-semibold line-clamp-4"
        >
          {title}
        </h2>
      </div>
      <div>
        {
          imageUrl ? (
            <LazyImage
              src={imageUrl}
              alt=""
              class="linked-image-hover__media h-full w-full object-contain object-bottom"
              loading={imageLoading}
              transitionName={transitionName}
            />
          ) : (
            <div class="h-full w-full bg-base-300" />
          )
        }
      </div>
    </div>

    <a href={href} class="absolute inset-0 z-1" aria-label={title}></a>
  </div>
</div>
