---
import Layout from "../Layouts/Layout.astro";
import LatestInterview from "../components/HomePage/LatestInterview.astro";
import picture1 from "../assets/images/culture.jpg";
import { articlesApi, interviewsApi, flippersApi } from "@/lib/api/api";

const CATEGORY_LABELS: Record<string, string> = {
  culture: "Культура",
  hotContent: "Самое Читаемое",
};

const CULTURE_CATEGORY_KEY = "culture";
const HOT_CONTENT_CATEGORY_KEY = "hotContent";

const normalizeCategory = (value?: string) => {
  if (!value) return null;
  const lowerCaseValue = value.toLowerCase().trim();
  const entry = Object.entries(CATEGORY_LABELS).find(
    ([key, label]) =>
      key.toLowerCase() === lowerCaseValue ||
      label.toLowerCase() === lowerCaseValue,
  );
  return entry ? entry[0] : lowerCaseValue;
};

const TAG_LABEL_ALIASES: Record<string, string> = {
  exhibition: "Выставка",
  exhibitions: "Выставка",
  concert: "Концерт",
  concerts: "Концерт",
  performance: "Спектакль",
  performances: "Спектакль",
  theatre: "Театр",
  theater: "Театр",
  theatres: "Театры",
  theaters: "Театры",
  culture: "Культура",
  paris: "Париж",
};

const isHotContent = (item: any) =>
  Boolean(item?.isHotContent) ||
  normalizeCategory(item?.category) === HOT_CONTENT_CATEGORY_KEY;

const isMainInCategoryItem = (item: any) =>
  item?.isMainInCategory === true ||
  item?.isMainInCategory === "true" ||
  item?.isMainInCategory === 1 ||
  item?.isMainInCategory === "1";

const getDate = (timestamp: any): Date => {
  if (timestamp instanceof Date) {
    return timestamp;
  }
  if (timestamp && typeof timestamp === "object" && "_seconds" in timestamp) {
    return new Date((timestamp as { _seconds: number })._seconds * 1000);
  }
  return new Date(timestamp);
};

const formatDate = (value: unknown) => {
  const date = getDate(value);
  if (Number.isNaN(date.getTime())) {
    return "";
  }
  return date
    .toLocaleDateString("ru-RU", {
      day: "2-digit",
      month: "long",
      year: "numeric",
    })
    .replace(" г.", "");
};

const normalizeTags = (value: unknown): string[] => {
  if (!Array.isArray(value)) {
    return [];
  }
  return value
    .map((tag) => (typeof tag === "string" ? tag.trim() : ""))
    .filter(Boolean);
};

const getPrimaryTag = (item: any): string => {
  if (item?.contentType === "interview") {
    return "Интервью";
  }
  if (item?.isNews) {
    return "Новости";
  }
  const tags = normalizeTags(item?.tags);
  if (tags.length === 0) {
    return "";
  }
  const firstTag = tags[0];
  const normalized = firstTag.toLowerCase();
  return TAG_LABEL_ALIASES[normalized] ?? firstTag;
};

const toContentHref = (item: any) => {
  if (item?.contentType === "interview") {
    return `/interviews/${item.id}`;
  }
  if (item?.contentType === "flipper") {
    return `/flippers/${item.id}`;
  }
  return `/${item?.isNews ? "news" : "article"}/${item.id}`;
};

const truncateText = (text?: string, maxLength: number = 220) => {
  if (!text) return "";
  if (text.length <= maxLength) return text;
  const truncated = text.slice(0, maxLength).trim();
  const lastSpaceIndex = truncated.lastIndexOf(" ");
  const safeText =
    lastSpaceIndex > 0 ? truncated.slice(0, lastSpaceIndex) : truncated;
  return `${safeText.trim()}...`;
};

const getTransitionName = (item: any) => {
  if (!item?.id) {
    return undefined;
  }
  const href = typeof item?.href === "string" ? item.href : "";
  if (href.startsWith("/events/")) {
    return `event-image-${item.id}`;
  }
  if (
    href.startsWith("/article/") ||
    href.startsWith("/news/") ||
    href.startsWith("/interviews/") ||
    href.startsWith("/flippers/")
  ) {
    return `main-article-image-${item.id}`;
  }
  return undefined;
};

const extractInterviewQuote = (interview: any): string | null => {
  if (!interview) return null;
  if (interview.mainQuote) return interview.mainQuote;
  if (!Array.isArray(interview.content)) return interview.lead ?? null;
  const quoteBlock = interview.content.find(
    (block: any) => block.type === "quote" && block.text,
  );
  if (quoteBlock) return quoteBlock.text;
  const qaBlock = interview.content.find(
    (block: any) => block.type === "qa" && block.answer,
  );
  return qaBlock?.answer ?? interview.lead ?? null;
};

let primaryCultureArticle: any = null;
let secondaryCultureArticle: any = null;
let tertiaryCultureArticle: any = null;
let editorialSidebarItems: any[] = [];
let cultureFeed: any[] = [];
let featuredInterview: any = null;

try {
  const [articles, interviews, flippers] = await Promise.all([
    articlesApi.list(),
    interviewsApi.list(),
    flippersApi.list(),
  ]);

  const allContent = [
    ...articles.map((article) => ({
      ...article,
      contentType: "article",
      tags: normalizeTags(article?.tags),
    })),
    ...interviews.map((interview) => ({
      ...interview,
      contentType: "interview",
      category: "culture",
      tags: normalizeTags(interview?.tags),
    })),
    ...flippers.map((flipper) => ({
      ...flipper,
      contentType: "flipper",
      imageUrl: flipper?.carouselContent?.[0]?.imageUrl || null,
      tags: normalizeTags(flipper?.tags),
    })),
  ];

  const sortedContent = [...allContent].sort(
    (a, b) => getDate(b.createdAt).getTime() - getDate(a.createdAt).getTime(),
  );

  const allCultureItems = sortedContent
    .filter((item) => normalizeCategory(item.category) === CULTURE_CATEGORY_KEY)
    .map((item) => ({
      ...item,
      href: toContentHref(item),
    }));

  const topItems = allCultureItems.filter(
    (item) => !isHotContent(item) && item.contentType !== "interview",
  );

  primaryCultureArticle =
    topItems.find((item) => isMainInCategoryItem(item)) ?? topItems[0] ?? null;

  const topWithoutPrimary = topItems.filter((item) =>
    primaryCultureArticle ? item.id !== primaryCultureArticle.id : true,
  );

  secondaryCultureArticle = topWithoutPrimary[0] ?? null;
  tertiaryCultureArticle = topWithoutPrimary[1] ?? null;

  const topIds = new Set(
    [
      primaryCultureArticle?.id,
      secondaryCultureArticle?.id,
      tertiaryCultureArticle?.id,
    ].filter(Boolean),
  );

  featuredInterview =
    allCultureItems.find(
      (item) => item.contentType === "interview" && !topIds.has(item.id),
    ) ??
    allCultureItems.find((item) => item.contentType === "interview") ??
    null;

  const excludedIds = new Set(
    [...topIds, featuredInterview?.id].filter(Boolean),
  );

  editorialSidebarItems = allCultureItems
    .filter((item) => !excludedIds.has(item.id))
    .slice(0, 4);

  const sidebarIds = new Set(editorialSidebarItems.map((item) => item.id));

  cultureFeed = allCultureItems.filter(
    (item) => !excludedIds.has(item.id) && !sidebarIds.has(item.id),
  );
} catch (error) {
  console.error("Failed to fetch culture page articles:", error);
}

const interviewQuote = extractInterviewQuote(featuredInterview);
const secondaryStories = [
  secondaryCultureArticle,
  tertiaryCultureArticle,
].filter(Boolean);
const editorialGridItems = cultureFeed.slice(0, 8);
const featuredInterviewName =
  featuredInterview?.interviewee ?? featuredInterview?.title ?? "";
---

<Layout
  navbarVariant="default"
  includePreFooterCTA={false}
  title="Культура - Maag"
  useClientRouter={true}
>
  <main
    class="min-h-screen bg-base-200 font-oswald text-[#302821] [background-image:radial-gradient(circle_at_top_center,rgba(255,255,255,0.82),transparent_48%)]"
  >
    <div
      class="mx-auto max-w-7xl px-4 pt-8 pb-20 sm:px-6 lg:px-8 lg:pt-10 lg:pb-[6.5rem]"
    >
      <h1 class="m-0 text-[1.05rem] font-medium uppercase tracking-[0.5em]">
        Культура
      </h1>

      <section
        class="mt-6 grid gap-7 lg:grid-cols-[minmax(0,1fr)_14rem] lg:items-start"
      >
        <div class="grid gap-8">
          {
            primaryCultureArticle ? (
              <article class="grid overflow-hidden bg-white/70 lg:min-h-[21rem] lg:grid-cols-[minmax(0,1.45fr)_minmax(16rem,0.95fr)]">
                <a
                  href={primaryCultureArticle.href}
                  class="block overflow-hidden"
                  aria-label={primaryCultureArticle.title}
                >
                  <img
                    src={primaryCultureArticle.imageUrl || picture1.src}
                    alt={primaryCultureArticle.title}
                    class="block h-full min-h-[15rem] w-full object-cover lg:min-h-[21rem]"
                    loading="lazy"
                    transition:name={getTransitionName(primaryCultureArticle)}
                  />
                </a>

                <div class="grid content-start gap-[0.55rem] bg-white/90 p-[1.2rem] lg:px-[1.3rem] lg:pt-[1.35rem] lg:pb-[1.1rem]">
                  <p class="m-0">
                    <span class="font-inter text-[0.58rem] font-semibold uppercase tracking-[0.3em] text-error">
                      {getPrimaryTag(primaryCultureArticle) || "Культура"}
                    </span>
                  </p>

                  <a
                    href={primaryCultureArticle.href}
                    class="text-current no-underline hover:underline [text-decoration-thickness:1px] underline-offset-[0.14em]"
                  >
                    <h2 class="m-0 max-w-[11ch] text-[clamp(1.9rem,3.5vw,2.8rem)] font-normal leading-[1.04]">
                      {truncateText(primaryCultureArticle.title, 96)}
                    </h2>
                  </a>

                  {primaryCultureArticle.lead && (
                    <p class="m-0 text-base leading-[1.55] text-[#302821bd]">
                      {truncateText(primaryCultureArticle.lead, 280)}
                    </p>
                  )}

                  <p class="mt-[0.4rem] font-inter text-[0.68rem] uppercase tracking-[0.18em] text-[#30282185]">
                    {formatDate(primaryCultureArticle.createdAt)}
                  </p>
                </div>
              </article>
            ) : (
              <article class="border border-[#3028211a] bg-white/70 p-5 text-[1.05rem] font-light leading-[1.5]">
                Пока нет материалов категории «Культура».
              </article>
            )
          }

          {
            secondaryStories.length > 0 && (
              <div class="grid gap-4 sm:grid-cols-2">
                {secondaryStories.map((item) => (
                  <article class="grid gap-[0.8rem]">
                    <a
                      href={item.href}
                      class="block aspect-[1.3/1] overflow-hidden"
                      aria-label={item.title}
                    >
                      <img
                        src={item.imageUrl || picture1.src}
                        alt={item.title}
                        class="block h-full w-full object-cover"
                        loading="lazy"
                        transition:name={getTransitionName(item)}
                      />
                    </a>

                    <div class="grid gap-[0.55rem]">
                      <p class="m-0">
                        <span class="font-inter text-[0.58rem] font-semibold uppercase tracking-[0.3em] text-error">
                          {getPrimaryTag(item) || "Культура"}
                        </span>
                      </p>

                      <a
                        href={item.href}
                        class="text-current no-underline hover:underline [text-decoration-thickness:1px] underline-offset-[0.14em]"
                      >
                        <h3 class="m-0 text-[1.4rem] font-normal leading-[1.12]">
                          {truncateText(item.title, 88)}
                        </h3>
                      </a>

                      {item.lead && (
                        <p class="m-0 text-[0.98rem] leading-[1.5] text-[#302821bd]">
                          {truncateText(item.lead, 120)}
                        </p>
                      )}
                    </div>
                  </article>
                ))}
              </div>
            )
          }
        </div>

        <aside class="min-w-0">
          <div
            class="grid gap-4 border-t border-[#3028211f] pt-4 lg:border-t-0 lg:border-l lg:border-[#3028211f] lg:pt-0 lg:pl-[1.1rem]"
          >
            <p
              class="m-0 text-[0.82rem] font-medium uppercase tracking-[0.38em]"
            >
              Нетленка
            </p>

            {
              editorialSidebarItems.length > 0 ? (
                editorialSidebarItems.map((item) => (
                  <article class="grid gap-[0.65rem] border-b border-[#3028211a] pb-4">
                    <a
                      href={item.href}
                      class="block aspect-[1.1/0.82] overflow-hidden"
                      aria-label={item.title}
                    >
                      <img
                        src={item.imageUrl || picture1.src}
                        alt={item.title}
                        class="block h-full w-full object-cover"
                        loading="lazy"
                        transition:name={getTransitionName(item)}
                      />
                    </a>

                    <div class="grid gap-[0.55rem]">
                      <p class="m-0">
                        <span class="font-inter text-[0.58rem] font-semibold uppercase tracking-[0.3em] text-error">
                          {getPrimaryTag(item) || "Культура"}
                        </span>
                      </p>

                      <a
                        href={item.href}
                        class="text-current no-underline hover:underline [text-decoration-thickness:1px] underline-offset-[0.14em]"
                      >
                        <h3 class="m-0 text-base font-normal leading-[1.16]">
                          {truncateText(item.title, 70)}
                        </h3>
                      </a>
                    </div>
                  </article>
                ))
              ) : (
                <p class="m-0 font-inter text-[0.88rem] leading-[1.5] text-[#3028219e]">
                  Пока нет материалов для боковой колонки.
                </p>
              )
            }
          </div>
        </aside>
      </section>

      {
        featuredInterview && interviewQuote && (
          <section class="mt-[2.2rem]">
            <LatestInterview
              tag="Интервью"
              interviewee={featuredInterviewName}
              quote={truncateText(interviewQuote, 130)}
              imageUrl={featuredInterview.imageUrl || picture1.src}
              href={`/interviews/${featuredInterview.id}`}
              transitionName={`main-article-image-${featuredInterview.id}`}
            />
          </section>
        )
      }

      <section class="mt-[1.6rem] border-t border-[#3028211f] pt-[1.6rem]">
        {
          editorialGridItems.length > 0 ? (
            <div class="grid grid-cols-2 gap-x-4 gap-y-[1.4rem] lg:grid-cols-4 lg:gap-x-5 lg:gap-y-[1.9rem]">
              {editorialGridItems.map((item) => (
                <article
                  class:list={[
                    "grid gap-[0.8rem]",
                    item.contentType === "interview" && "content-start",
                  ]}
                >
                  <a
                    href={item.href}
                    class:list={[
                      "block overflow-hidden bg-white/50 aspect-[1.02/1]",
                      item.contentType === "interview" &&
                        "mx-auto w-full max-w-[12rem] aspect-square rounded-full bg-transparent",
                    ]}
                    aria-label={item.title}
                  >
                    <img
                      src={item.imageUrl || picture1.src}
                      alt={item.title}
                      class:list={[
                        "block h-full w-full object-cover",
                        item.contentType === "interview" && "rounded-full",
                      ]}
                      loading="lazy"
                      transition:name={getTransitionName(item)}
                    />
                  </a>

                  <div
                    class:list={[
                      "grid gap-[0.55rem]",
                      item.contentType === "interview" &&
                        "mx-auto w-full max-w-[12rem]",
                    ]}
                  >
                    <p class="m-0">
                      <span class="font-inter text-[0.58rem] font-semibold uppercase tracking-[0.3em] text-error">
                        {getPrimaryTag(item) || "Культура"}
                      </span>
                    </p>

                    <a
                      href={item.href}
                      class="text-current no-underline hover:underline [text-decoration-thickness:1px] underline-offset-[0.14em]"
                    >
                      <h3 class="m-0 text-[1.18rem] font-normal leading-[1.18]">
                        {truncateText(item.title, 72)}
                      </h3>
                    </a>
                  </div>
                </article>
              ))}
            </div>
          ) : (
            <article class="bg-white/70 p-5 text-[1.05rem] font-light leading-[1.5]">
              Больше материалов пока нет.
            </article>
          )
        }
      </section>
    </div>
  </main>
</Layout>
