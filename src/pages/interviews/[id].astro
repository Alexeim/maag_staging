---
import Layout from "../../Layouts/Layout.astro";
import { interviewsApi } from "../../lib/api/api";
import ArticleBody from "../../components/article/ArticleBody.astro";
import ContentCollectionMarquee from "@/components/article/ContentCollectionMarquee.astro";
import ContentActions from "@/components/common/ContentActions.astro";
import InterviewHero from "@/components/interview/InterviewHero.astro";
import { getKnownTagLabel } from "@/content/tags/parisTags";
import picture1 from "@/assets/images/culture.jpg";
import {
  buildContentCollectionItems,
  buildLinkedContentLookup,
  fetchPublicContentPools,
  type LinkedContentLookup,
  toDate,
} from "@/lib/utils/contentCollectionMarquee";
import { buildArticleStructuredData } from "@/lib/seo/structuredData";

const { id } = Astro.params;

let interview = null;
try {
  if (id) {
    interview = await interviewsApi.getById(id);
  }
} catch (error) {
  console.error("Failed to fetch interview:", error);
}

if (!interview) {
  return Astro.redirect("/404");
}

if ((interview as any).published !== true) {
  return Astro.redirect("/404");
}

console.log("here --------------------->>>>>>>>>", Object.keys(interview));

const {
  title,
  author,
  interviewee,
  mainQuote,
  content,
  imageUrl,
  imageCaption,
  lead,
} = interview;
const authorName = author ? `${author.firstName} ${author.lastName}` : "MAAG";
const firstBookmarkTag =
  Array.isArray((interview as any)?.tags) &&
  typeof (interview as any).tags[0] === "string"
    ? (interview as any).tags[0]
    : "";
const bookmarkTag = firstBookmarkTag ? getKnownTagLabel(firstBookmarkTag) : "";
const siteOrigin = Astro.site?.origin ?? Astro.url.origin;
const articleStructuredData = buildArticleStructuredData({
  siteOrigin,
  path: `/interviews/${id}`,
  headline: title,
  description: lead,
  image: imageUrl,
  datePublished: interview.createdAt,
  dateModified: (interview as any).updatedAt ?? interview.createdAt,
  authorName: author ? authorName : undefined,
});

let collectionContentItems: any[] = [];
let recentInterviewItems: any[] = [];
let linkedContentLookup: LinkedContentLookup = {};

try {
  const pools = await fetchPublicContentPools();
  linkedContentLookup = buildLinkedContentLookup(pools);
  const allInterviews = pools.allInterviews;
  collectionContentItems = await buildContentCollectionItems({
    contentCollectionId: (interview as any)?.contentCollectionId,
    currentContentType: "interview",
    currentId: id,
    pools,
  });

  const relatedInterviewIds = new Set(
    collectionContentItems
      .filter((i) => i.contentType === "interview")
      .map((i) => i.id),
  );
  recentInterviewItems = allInterviews
    .filter((i) => i.id !== id && !relatedInterviewIds.has(i.id))
    .sort(
      (a, b) =>
        (toDate(b?.createdAt)?.getTime() ?? 0) -
        (toDate(a?.createdAt)?.getTime() ?? 0),
    )
    .slice(0, 12)
    .map((item) => ({ ...item }));
} catch (e) {
  console.error("Failed to fetch collection content for interview page:", e);
}
---

<Layout
  navbarVariant="default"
  includePreFooterCTA={false}
  title={`${title} - Maag`}
  description={lead}
  image={imageUrl}
  canonicalPath={`/interviews/${id}`}
  type="article"
  structuredData={articleStructuredData}
  useClientRouter={true}
>
  <main class="bg-base-200 min-h-screen">
    <div class="max-w-[1512px] mx-auto">
      <InterviewHero
        category="Интервью"
        title={title}
        lead={lead}
        authorName={authorName}
        authorAvatar={author?.avatar}
        interviewee={interviewee}
        mainQuote={mainQuote || ""}
        createdAt={interview.createdAt}
        imageUrl={imageUrl}
        imageAlt={imageCaption || title}
        imageCaption={imageCaption}
        transitionName={`main-article-image-${id}`}
        heroOrientation={interview?.heroOrientation ?? "image-left"}
      />

      <div class="px-4 pb-[100px] pt-10 sm:px-6 lg:px-14 lg:pt-16">
        <div class="mx-auto mb-10 max-w-[826px]">
          <ContentActions
            contentType="interview"
            contentId={id!}
            title={title}
            href={`/interviews/${id}`}
            tag={bookmarkTag}
            imageUrl={imageUrl}
            align="center"
          />
        </div>
        <ArticleBody
          contentBlocks={Array.isArray(content) ? content : []}
          linkedContentLookup={linkedContentLookup}
        />
        <ContentCollectionMarquee
          collectionItems={collectionContentItems}
          categoryItems={recentInterviewItems}
          fallbackImageSrc={picture1.src}
        />
      </div>
    </div>
  </main>
</Layout>
